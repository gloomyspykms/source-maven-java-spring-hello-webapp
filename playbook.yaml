---
- hosts: localhost
  connection : local
  tasks:
    - docker_container: 
        name: myhello
        image: yerimaura/myhello:latest
        state: started
        ports:
           - "80:8080"  
...

